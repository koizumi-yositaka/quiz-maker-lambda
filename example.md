# HTTPSとは

**HTTPS (HyperText Transfer Protocol Secure)** は、SSL/TLSという暗号化プロトコルを用いてインターネット上でデータをやり取りするための通信プロトコルで、セキュリティ機能を備えた **HTTP (HyperText Transfer Protocol)** の拡張版です。

# SSL/TLS とは

**SSL/TLS**（Secure Sockets Layer / Transport Layer Security）はインターネットなど、コンピュータ間の通信において、セキュアな通信を実現するためのプロトコルです

元々はSSLという名前だったが、脆弱性が見つかりTLSが新しく設計されました。TLSを使っていても、過去の名残でSSL/TLSと言われています。

- 通信内容の暗号化
- 改ざんの検出
- 通信相手の認証

を実現しています。

> 暗号化通信
> 

SSL証明書を用いたハイブリッド暗号化方式(別ナレッジ)により暗号化が実現されています。

受信側が用意する公開鍵がSSLサーバ証明書に含まれています。

このクライアントとサーバ側で共通鍵を交換する手順を「ハンドシェイク」と言います。

> データの完全性(改ざんの防止)
> 

メッセージ認証コード（MAC）という技術が使われています。

MAC は、データが改ざんされていないことを保証し、さらにそのデータが特定の送信者から送られたものであることを確認するための技術です。

送信者がデータと共通鍵を使ってMACを生成し、データと一緒に送信します。その後受信者は、受け取ったデータと共通鍵を使ってMACを再計算します。送信者が送ったMACと受信者が計算したMACを比較し、一致すればデータが改ざんされていないことの証明になります。

また、データが正当な送信者から送られたものであることを保証することもできます。

> Webサイトの認証
> 

webサーバはCA（Certificate Authority：認証局）に、そのウェブサイトが正当なものであることを保証する**デジタル証明書**を発行してもらいます。サーバーを公開する際にこの証明書を配置することが必要です。

デジタル証明書は、公開鍵とその所有者を証明するものです。

クライアントから、webサーバに接続要求があった際にサーバーから証明書が送信されます。クライアント側でこの証明書が正しいかを確認することで接続しようとしているWebサイトが正しいものかを判断します。

かなり複雑に感じますが、SSL/TLS は何を実現する技術なのか覚えておきたいです。